/**
 * Platform module: Connections, operations, outbox, webhooks, inbound API, SAP client (§8).
 */
@ApplicationModule(displayName = "Platform · integration")
package org.gcc.usp.platform.integration;

import org.springframework.modulith.ApplicationModule;
