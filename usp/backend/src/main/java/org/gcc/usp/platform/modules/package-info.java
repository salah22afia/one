/**
 * Platform module: Module & feature registry, toggles and module-scoped permissions (§4).
 */
@ApplicationModule(displayName = "Platform · modules")
package org.gcc.usp.platform.modules;

import org.springframework.modulith.ApplicationModule;
