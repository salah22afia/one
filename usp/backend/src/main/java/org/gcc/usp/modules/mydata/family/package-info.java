/**
 * Feature mydata.family: family members and dependants with the expiry of their documents (SAP-003), read live and
 * never stored. Adding or changing one is a service of the MD domain.
 */
package org.gcc.usp.modules.mydata.family;
