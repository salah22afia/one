/**
 * Feature timeleave.balances: My balances — the employee's absence quotas with entitlement, used and remaining
 * (SAP-004), read live and never stored.
 */
package org.gcc.usp.modules.timeleave.balances;
