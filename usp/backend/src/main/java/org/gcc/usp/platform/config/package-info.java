/**
 * Platform module: Generic configuration versioning: draft, schedule, second approval, rebase, correct, revert (§7.10).
 */
@ApplicationModule(displayName = "Platform · config")
package org.gcc.usp.platform.config;

import org.springframework.modulith.ApplicationModule;
