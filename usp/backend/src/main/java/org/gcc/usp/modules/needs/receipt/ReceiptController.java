package org.gcc.usp.modules.needs.receipt;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/** REST API of feature needs.receipt. Keep the feature's service, entities and repository in this package. */
@RestController
@RequestMapping("/api/v1/needs/receipt")
class ReceiptController {
}
