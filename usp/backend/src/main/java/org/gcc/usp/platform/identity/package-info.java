/**
 * Platform module: OIDC login, current user, SU01 mapping and principal propagation to SAP (§11).
 */
@ApplicationModule(displayName = "Platform · identity")
package org.gcc.usp.platform.identity;

import org.springframework.modulith.ApplicationModule;
