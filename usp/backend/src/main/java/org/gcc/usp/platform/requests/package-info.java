/**
 * Platform module: Request runtime: submit, inbox, timeline, drafts, resubmit, withdraw.
 */
@ApplicationModule(displayName = "Platform · requests")
package org.gcc.usp.platform.requests;

import org.springframework.modulith.ApplicationModule;
