/**
 * Feature mydata.card: the digital employee card. Its QR carries a short-lived code signed by the portal (employee
 * number, name, expiry); the public verification page checks the signature — nothing stored, no SAP call.
 */
package org.gcc.usp.modules.mydata.card;
