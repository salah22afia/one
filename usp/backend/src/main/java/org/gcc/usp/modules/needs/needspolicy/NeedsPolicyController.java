package org.gcc.usp.modules.needs.needspolicy;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/** REST API of feature needs.needs-policy. Keep the feature's service, entities and repository in this package. */
@RestController
@RequestMapping("/api/v1/needs/needs-policy")
class NeedsPolicyController {
}
