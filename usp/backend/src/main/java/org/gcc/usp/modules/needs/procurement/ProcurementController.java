package org.gcc.usp.modules.needs.procurement;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/** REST API of feature needs.procurement. Keep the feature's service, entities and repository in this package. */
@RestController
@RequestMapping("/api/v1/needs/procurement")
class ProcurementController {
}
