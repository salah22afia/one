package org.gcc.usp.modules.needs.needrequest;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/** REST API of feature needs.need-request. Keep the feature's service, entities and repository in this package. */
@RestController
@RequestMapping("/api/v1/needs/need-request")
class NeedRequestController {
}
