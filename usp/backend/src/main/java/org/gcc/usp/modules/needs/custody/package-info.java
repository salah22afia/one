/**
 * Feature needs.custody: Custody register, exposed to other modules (CU-01, CU-02).
 * Exposed as a named interface so other modules (e.g. lifecycle clearance) can use it (B2).
 */
@NamedInterface("custody")
package org.gcc.usp.modules.needs.custody;

import org.springframework.modulith.NamedInterface;
