/**
 * Platform module: the service catalogue (CAT-01) — domains, services and their waves, the Home dock and "notify me" —
 * kept in PostgreSQL and administered in the admin portal (operational configuration, logged in catalog.change_log).
 */
@ApplicationModule(displayName = "Platform · catalog")
package org.gcc.usp.platform.catalog;

import org.springframework.modulith.ApplicationModule;
