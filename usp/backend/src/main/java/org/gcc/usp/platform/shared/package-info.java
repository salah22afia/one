/**
 * Platform module: shared kernel — localized text and message catalogs, API errors, sequences. Open to every module.
 */
@ApplicationModule(displayName = "Platform · shared",
        type = ApplicationModule.Type.OPEN)
package org.gcc.usp.platform.shared;

import org.springframework.modulith.ApplicationModule;
