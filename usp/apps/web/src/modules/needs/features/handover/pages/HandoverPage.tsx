import { Placeholder } from '@usp/ui-web';

/** needs.handover: Handover notes per batch (ND-20). */
export default function HandoverPage() {
  return <Placeholder title={{ ar: "التسليم", en: "Handover" }} feature="needs.handover" />;
}
