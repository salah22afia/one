import { Placeholder } from '@usp/ui-web';

/** needs.custody: Custody register, exposed to other modules (CU-01, CU-02). */
export default function CustodyPage() {
  return <Placeholder title={{ ar: "عهدتي", en: "My custody" }} feature="needs.custody" />;
}
