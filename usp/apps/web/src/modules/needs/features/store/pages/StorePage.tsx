import { Placeholder } from '@usp/ui-web';

/** needs.store: Store decision, reservation, store desk (ND-08, ND-22a). */
export default function StorePage() {
  return <Placeholder title={{ ar: "مكتب المستودع", en: "Store desk" }} feature="needs.store" />;
}
