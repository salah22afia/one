import { Placeholder } from '@usp/ui-web';

/** timeleave.leave-cancellation: Cancellation of approved leave (LV-08). */
export default function LeaveCancellationPage() {
  return <Placeholder title={{ ar: "إلغاء إجازة معتمدة", en: "Leave cancellation" }} feature="timeleave.leave-cancellation" />;
}
