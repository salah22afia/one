import { Placeholder } from '@usp/ui-web';

/** timeleave.leave-history: Merged from SAP absences and portal requests (LV-09). */
export default function LeaveHistoryPage() {
  return <Placeholder title={{ ar: "سجل إجازاتي", en: "Leave history" }} feature="timeleave.leave-history" />;
}
