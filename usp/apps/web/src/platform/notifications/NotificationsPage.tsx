import { Placeholder } from '@usp/ui-web';

export default function NotificationsPage() {
  return <Placeholder title={{ ar: 'التنبيهات', en: 'Notifications' }} feature="platform.notifications" />;
}
