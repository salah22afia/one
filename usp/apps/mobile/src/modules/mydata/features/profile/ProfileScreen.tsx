import { Placeholder } from '../../../../shared/Placeholder';

/** mydata.profile: Personal data, balances, documents, payslips, dependants (ME-01). */
export default function ProfileScreen() {
  return <Placeholder title={{ ar: "ملفي", en: "My profile" }} feature="mydata.profile" />;
}
