import { Placeholder } from '../../../../shared/Placeholder';

/** timeleave.leave-request: Type picker, live evaluation, pay tiers, entitlements, IT2001 posting (LV-01…LV-07). */
export default function LeaveRequestScreen() {
  return <Placeholder title={{ ar: "طلب إجازة", en: "Leave request" }} feature="timeleave.leave-request" />;
}
