import { Placeholder } from '../../../../shared/Placeholder';

/** needs.receipt: Receipt reports in batches, committee signatures, remainder close (ND-18, ND-19). */
export default function ReceiptScreen() {
  return <Placeholder title={{ ar: "الاستلام", en: "Receipt" }} feature="needs.receipt" />;
}
