import { Placeholder } from '../../../../shared/Placeholder';

/** needs.specification: Technical entity: specify, provide from pool, reroute (ND-06, ND-07). */
export default function SpecificationScreen() {
  return <Placeholder title={{ ar: "تحديد الصنف", en: "Specification" }} feature="needs.specification" />;
}
