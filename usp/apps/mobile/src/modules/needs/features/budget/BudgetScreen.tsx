import { Placeholder } from '../../../../shared/Placeholder';

/** needs.budget: Earmarked funds reservation, top-up, release (ND-11). */
export default function BudgetScreen() {
  return <Placeholder title={{ ar: "الموازنة", en: "Budget" }} feature="needs.budget" />;
}
