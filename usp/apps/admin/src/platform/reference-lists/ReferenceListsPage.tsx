import { Placeholder } from '@usp/ui-web';

export default function ReferenceListsPage() {
  return <Placeholder title={{ ar: 'القوائم المرجعية', en: 'Reference lists' }} feature="admin.reference-lists" />;
}
