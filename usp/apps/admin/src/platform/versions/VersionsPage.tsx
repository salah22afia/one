import { Placeholder } from '@usp/ui-web';

export default function VersionsPage() {
  return <Placeholder title={{ ar: 'الإصدارات والاعتماد', en: 'Versions & approvals' }} feature="admin.versions" />;
}
