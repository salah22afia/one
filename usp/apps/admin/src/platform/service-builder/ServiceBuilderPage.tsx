import { Placeholder } from '@usp/ui-web';

export default function ServiceBuilderPage() {
  return <Placeholder title={{ ar: 'منشئ الخدمات', en: 'Service builder' }} feature="admin.service-builder" />;
}
