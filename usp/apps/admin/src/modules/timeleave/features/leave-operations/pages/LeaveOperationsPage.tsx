import { Placeholder } from '@usp/ui-web';

/** timeleave.leave-operations: Seasonal windows, entitlement groups, period close (OPS-01). */
export default function LeaveOperationsPage() {
  return <Placeholder title={{ ar: "تشغيل الإجازات", en: "Leave operations" }} feature="timeleave.leave-operations" />;
}
