import { Placeholder } from '@usp/ui-web';

/** timeleave.leave-policy: Versioned types, routes, entitlements, calendar (POL-01…POL-04). */
export default function LeavePolicyPage() {
  return <Placeholder title={{ ar: "سياسة الإجازات", en: "Leave policy" }} feature="timeleave.leave-policy" />;
}
