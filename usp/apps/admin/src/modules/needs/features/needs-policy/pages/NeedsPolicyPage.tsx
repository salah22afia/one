import { Placeholder } from '@usp/ui-web';

/** needs.needs-policy: Versioned needs policy, operations and simulator. */
export default function NeedsPolicyPage() {
  return <Placeholder title={{ ar: "سياسة الاحتياج", en: "Needs policy" }} feature="needs.needs-policy" />;
}
