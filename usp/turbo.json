{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "typecheck": { "dependsOn": ["^typecheck"] },
    "test": {},
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] }
  }
}
